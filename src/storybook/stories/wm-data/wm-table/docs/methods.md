# Methods

<details open>
  <summary>Methods</summary>
    <div>
        | Method | Parameters | Return Type | Description |
        |--------|------------|------------|-------------|
        | `refreshData` | None | void | To refresh the data in data table with the currently applied filters and sort. |
        | `clearFilter` | None | void | To clear the filters applied on data table. |
        | `focus` | None | void | To focus on a field in edit mode(applicable to inline and quick edit data tables alone). |
        | `redraw` | boolean | void | To force re-render Data Table. |
        | `selectItem` | any | void | To select a row, The parameter can be index or object of row |
        | `deselectItem` | any | void | To deselect a row, The parameter can be index or object of row. **Note**: Deselect will work only for data table with multiselect enabled on it |
        | `editRow` | None | void | Selected row will be edited (applicable to inline and quick edit data tables alone). |
        | `addRow` | None | void | To add a new row (applicable to inline and quick edit data tables alone). |
        | `saveRow` | None | void | To save a row (applicable to inline and quick edit data tables alone). |
        | `cancelRow` | None | void | To cancel a row edit (applicable to inline and quick edit data tables alone). |
        | `deleteRow` | None | void | To delete a row (applicable to inline and quick edit data tables alone). |
        | `hideEditRow` | None | void | To hide the edit row and go back to view mode (applicable to inline and quick edit data tables alone). |
    </div>
</details>


