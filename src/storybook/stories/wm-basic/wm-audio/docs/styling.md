# Styling

The Audio component uses the native audio player styling of the device. Custom styling options for the Audio component are limited as it primarily uses the device's native audio player controls.