# Methods

The Icon component can be accessed in script using `Page.Widgets.widgetName` syntax, where `widgetName` is the name you assigned to the icon instance.

The Icon component does not have any specific methods beyond the standard property accessors and mutators.