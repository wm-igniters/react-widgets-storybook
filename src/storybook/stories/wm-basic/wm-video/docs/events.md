# Callback Events

The Video widget does not expose specific callback events in the current documentation. Standard HTML5 video events may be available through the underlying implementation, but they are not formally documented as part of the WaveMaker Video widget API.